android {
    defaultConfig {
        applicationId "cc.aoeiuv020"
        applicationIdSuffix ".record"
        resValue("string", "app_name", "RecordDemo")
    }
}

dependencies {
    implementation 'androidx.constraintlayout:constraintlayout:' + constraint_layout_version
}
