android {
    defaultConfig {
        applicationId "cc.aoeiuv020.demo"
        applicationIdSuffix ".json"
        resValue("string", "app_name", "JsonDemo")
    }
}

dependencies {
    implementation 'com.android.support.constraint:constraint-layout:' + constraint_layout_version
    implementation project(':gson')
}
